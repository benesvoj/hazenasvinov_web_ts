import React from 'react';

function MyProfileAndSettings() {

    // const {openPopper, referenceRef, popperProps, Popper, closePopper, isOpen} = useAnimatedPopper({
    //     placement: 'bottom-start',
    // });

    return (
        <div>
        </div>
    );
}

export default MyProfileAndSettings;